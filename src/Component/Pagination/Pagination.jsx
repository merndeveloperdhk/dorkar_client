


const Pagination = () => {
    /* const [products, setProducts] = useState([]);
  useEffect( () => {
    fetch('http://localhost:5000/addPost')
    .then(res => res.json())
    .then(data => setProducts(data))
  },[]) */
    return (
        <div >
            {/* <h1 className="hidden md:block">Showing 1-10 of {products.length} Products</h1> */}
            {/* <PaginationButton products={products}></PaginationButton> */}
        </div>
    );
};

export default Pagination;